//
//  Images.swift
//  MyBoxer
//
//  Created by Sebastian Hajduk on 21/09/2022.
//

import UIKit

enum Images {
    static let player = UIImage(named: "boxer") ?? UIImage()

    static let bag = UIImage(named: "skill") ?? UIImage()
    static let rank = UIImage(named: "belt") ?? UIImage()
    static let ring = UIImage(named: "ring") ?? UIImage()
    static let shop = UIImage(named: "shop") ?? UIImage()
    static let team = UIImage(named: "club") ?? UIImage()

    static let ballThrow = UIImage(named: "throwing") ?? UIImage()
    static let interval = UIImage(named: "running") ?? UIImage()
    static let jumpingRope = UIImage(named: "jumping") ?? UIImage()
    static let shadowBox = UIImage(named: "boxing") ?? UIImage()
    static let weightLift = UIImage(named: "lifting") ?? UIImage()

    static let opponent = UIImage(named: "opponent") ?? UIImage()

    static let coin = UIImage(systemName: "dollarsign.circle.fill") ?? UIImage()

    static let boxingGlove = UIImage(named: "boxing-gloves") ?? UIImage()
    static let boxingBoots = UIImage(named: "boxing-boots") ?? UIImage()
    static let boxingShorts = UIImage(named: "boxing-shorts") ?? UIImage()
    static let boxingTapes = UIImage(named: "boxing-tapes") ?? UIImage()
    
    static let managerImage = UIImage(named: "manager") ?? UIImage()
    static let coachImage = UIImage(named: "coach") ?? UIImage()
    static let physioImage = UIImage(named: "physio") ?? UIImage()
    static let cutmanImage = UIImage(named: "cutman") ?? UIImage()
}
